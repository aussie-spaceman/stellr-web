# Promote dev → main — 2026-09-15 (second promotion of the day)

Status: **Promoted** — #87 merged 2026-09-15 18:53Z as `87f7ccb` (merge commit); production `dpl_EucPmzWQLytAWDVFPZEgMgnZN5dN` READY 18:57Z; verified; `dev` fast-forwarded to `87f7ccb`

Rollback target: `dpl_9k2HjzNYjfmTRszeuvGSPwhZvMRu` — main @ `b18af0c`, READY, isRollbackCandidate=true
https://vercel.com/stellreducation/stellr-web/9k2HjzNYjfmTRszeuvGSPwhZvMRu

## Shipping (origin/main..origin/dev)
- `9ad818b` Stop local dev pointing at production (#78)
- `a10523c` Fix the flaky sign-out test; commit the promotion records (#79)
- `1a9d831` Hand over session 4: the open-items pass, and what is still the maintainer's (#80)
- `73e6393` Seed the grant rules dev never had; make the close-out tracker a repo file (#81)
- `5d1e373` Sign out through Clerk's testing helper; assert the sign-in redirect (#82)
- `ece9909` Dev Vercel project now tracks dev; close the tracker rows that closed today (#83)
- `efd5e9f` Tracker: first Production build on the dev project verified (4.7) (#85)

13 files, +848/−16. `dev..main` is three earlier promotion merge commits; `main`'s
tree equals the merge base (`ace3a33`), so no pre-sync was needed. Not included:
#84 (Stripe promotion codes) — open on `dev`, not merged.

## Blast radius
- Migrations: **NO**. `db:status --prod` lists `20260910140426/27` as "pending" —
  those are the 10 Sept grade migrations, applied and verified on 10 Sept, recorded
  in the ledger under MCP timestamps (`20260910230533/…0740`). Ledger-name mismatch
  only; follow-up: realign the two ledger rows.
- vercel.json crons: NO · env vars: NO · webhooks: NO · lib/email: NO
- Runtime code: **NONE**. `scripts/dev.mjs` + `scripts/production-guard.mjs` are
  local tooling outside the Next build; `e2e/` is test-only; `supabase/seed.sql`
  is dev-only (seeder refuses production); the rest is docs, skills, release
  records, `.env.local.example`.
- **Production behaviour is unchanged. No-op deploy.**

## CI (every e2e read by step outcome — `✓ Run npx playwright test`, 45 passed / 1 skipped)
- #78 run 34992689623 · #79 34992774285 · #80 34994737998 · #81 (1 flaky on the
  old sign-out test, then green) · #82 · #83 · #85 — all verify ✓ e2e ✓.
- Unit suite 593 (was 583).

## Steps
- [x] 1 What is shipping
- [x] 2 Blast radius
- [x] 3 Migrations — none to apply
- [x] 4 Rollback target
- [x] 5 Promotion PR — https://github.com/aussie-spaceman/stellr-web/pull/87 (also carries #86, this record)
- [x] 6 Merge — user approved in session; `gh pr merge 87 --merge` → `87f7ccb`. `Vercel – stellr-web` on the PR read "Deployment has completed" (not rate-limited) before merging.
- [x] 7 Verify production — www 200 · app 307 → /sign-in · `/api/cron/entitlements` → `{"error":"Unauthorized"}` · production deployment `dpl_EucPmzWQLytAWDVFPZEgMgnZN5dN` READY for `87f7ccb` · `Vercel – stellr-web-dev` for the same sha: "Canceled by Ignored Build Step"
- [x] 8 `dev` fast-forwarded to `87f7ccb` after main's own CI run (35010310688: verify ✓, e2e ✓ 45/1) — required now that `dev` enforces admins; `main..dev` 0 / `dev..main` 0
