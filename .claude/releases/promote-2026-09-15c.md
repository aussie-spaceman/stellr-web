# Promote dev → main — 2026-09-15 (third promotion of the day)

Status: **PR open** — awaiting user confirmation to merge

Rollback target: `dpl_7gmENarddEb6mezMmeKmrqf23XjS` — main @ `68cb24c`, READY, isRollbackCandidate=true
https://vercel.com/stellreducation/stellr-web/7gmENarddEb6mezMmeKmrqf23XjS

## Shipping (origin/main..origin/dev)
- `037fb5d` Cut Vercel Function Storage: build only main/dev, shrink the function bundles (#92)

14 files, +363/−434. `dev` is not behind `main`; no pre-sync needed.
Handover: `docs/handovers/HANDOVER-vercel-function-storage-2026-09-15.md`.

## Blast radius
- Migrations: **NO**. `db:status --prod` still lists `20260910140426/27` as
  pending — the known ledger-name mismatch (applied 10 Sept under MCP
  timestamps `20260910230533/…0740`). Nothing new in this diff.
- vercel.json: **changed** — `ignoreCommand` added; **crons untouched**.
  Effect on the production project: only `main` builds from now on. This
  promotion's own `main` build is the first to run under the rule (the ignore
  step exits 1 = build for `main`; verified on the branch push and the `dev`
  merge, where it exited 0 = skip on this project).
- env vars: NO (the script reads only Vercel system vars `VERCEL_PROJECT_ID`,
  `VERCEL_GIT_COMMIT_REF`).
- webhooks: `app/api/webhooks/google-sheets` shares `lib/google-sheets.ts`,
  whose Google client moved from `googleapis` to `@googleapis/sheets|drive` +
  `google-auth-library@11`. Same call surface; JWT + Drive + Calendar
  exercised read-only against the real service account before merge.
- lib/email / cron volume: NO. `cron/motion-bookings` imports
  `lib/google-calendar.ts` (same client swap; read-only calendar list).
- Runtime code: **YES** — five Google-backed routes (client swap), the
  `/lp/[slug]` OG card (Node → edge runtime, no `generateStaticParams`), and
  `next.config.mjs` tracing excludes (build-time only). Behaviour is meant to
  be identical; the change is what ships in the function bundle.

## Verification before merge
- CI on #92 (verify + e2e) green by step outcome on both commits.
- Local prod build: trace union 62.7 → 47.6 MB; routes > 4 MB 9 → 2.
- OG card rendered locally for both slugs + unknown slug: 200, 1200×630 PNG.
- `dev` alias built once on `stellr-web-dev` after the merge, READY.

## After merge — to fill in
- Production deployment id / state:
- www 200 / app 307 → /sign-in / cron 401:
- OG card on production (`/lp/homeschool-students/opengraph-image-*`):
- `/api/registrations/[id]/spreadsheet` or sheet-sync exercised? (needs a
  real registration; otherwise note untested in prod)
