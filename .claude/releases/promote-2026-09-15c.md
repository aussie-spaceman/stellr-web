# Promote dev → main — 2026-09-15 (third promotion of the day)

Status: **Promoted** — #91 merged 2026-09-15 20:29Z as `68cb24c` (merge commit); production `dpl_7gmENarddEb6mezMmeKmrqf23XjS` READY 20:32Z, aliased to www/app; verified; `dev` fast-forwarded to `68cb24c`

Rollback target: `dpl_EucPmzWQLytAWDVFPZEgMgnZN5dN` — main @ `87f7ccb`, READY, target=production, isRollbackCandidate=true
https://vercel.com/stellreducation/stellr-web/EucPmzWQLytAWDVFPZEgMgnZN5dN

## Shipping (origin/main..origin/dev)
- `3b37100` Release record: promoted (#87 → 87f7ccb); tracker 4.5 closed, 4.8–4.9 added (#88)
- `812b297` Let registration checkouts accept Stripe promotion codes (#84)
- `401b1be` Close out session 4: final section, tracker 4.10–4.11 (#89) — landed on `dev` while #91 was open; docs only

8 files, +20/−6 at open; #89 added docs only. `dev..main` is empty; no pre-sync needed.

## Why
Discount codes were configured in Stripe but the hosted Checkout page for
event registration showed no "Add promotion code" field. Stripe only renders
it when the session is created with `allow_promotion_codes: true`; none of
the 14 `checkout.sessions.create` calls in the repo passed it.

## Blast radius
- Migrations: **NO**. `db:status --prod` unchanged from the 15b record — the
  two "pending" are the 10 Sept grade migrations recorded under MCP
  timestamps; ledger-name mismatch only.
- vercel.json crons: NO · env vars: NO · lib/email: NO
- Webhooks: **contract unchanged**. Stripe Checkout Sessions from the five
  registration routes now accept promotion codes; `checkout.session.completed`
  still arrives at the same route with the same metadata, and the handler
  already records `session.amount_total` (post-discount).
- Runtime code: **YES**, narrow. One added param on `stripe.checkout.sessions.create`
  in `app/api/register/individual`, `app/api/register/group`,
  `lib/individual-payment.ts`, `app/api/members/teams/[id]/payment-link`,
  `app/api/members/billing/payment-link`. Community, store, and membership
  checkouts untouched (membership passes `discounts`, which Stripe will not
  combine with `allow_promotion_codes`).
- **User-visible change in production:** registration Checkout pages gain an
  "Add promotion code" link. Any live promotion code restrictions
  (product/customer/first-time/expiry) are enforced by Stripe.

## CI (read by step outcome)
- #84 run 35004654064 — verify: Typecheck ✓ Design-system lint ✓ Unit tests ✓
  Build ✓ · e2e: `Run npx playwright test` ✓. `Vercel – stellr-web` READY.
- Unit: `lib/individual-payment.test.ts` 8/8 incl. new
  `allow_promotion_codes: true` assertion.
- Not exercised in a browser on dev: both dev deployments sit behind Vercel
  SSO and the session had no bypass secret.

## Steps
- [x] 1 What is shipping
- [x] 2 Blast radius
- [x] 3 Migrations — none to apply
- [x] 4 Rollback target
- [x] 5 Promotion PR — https://github.com/aussie-spaceman/stellr-web/pull/91 (this record: PR #90 → dev)
- [x] 6 Merge — user approved in session ("promote it now"); user ran `gh pr merge 91 --merge` (classifier blocks merges from the session) → `68cb24c`, two parents. First attempt was refused by base-branch policy while `Vercel – stellr-web` was still queued (~19 min behind other builds); merged once it passed, no `--admin`.
- [x] 7 Verify production — www 200 · app 307 → /sign-in · `/api/cron/entitlements` → `{"error":"Unauthorized"}` · `verify:prod` Stripe key LIVE, valid · main's own CI 35019958231 verify ✓ (Typecheck/lint/Unit/Build) e2e ✓. **Open:** the promo-code link itself only appears on a newly created Checkout Session, which writes a production registration — left for the user to exercise.
- [x] 8 `dev` fast-forwarded to `68cb24c` after main's CI; `main..dev` 0 / `dev..main` 0
