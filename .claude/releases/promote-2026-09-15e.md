# Promote dev → main — 2026-09-15 (fifth promotion of the day; docs only)

Status: **Promoted** — #97 merged 2026-09-16 02:48Z as `bddd833` (merge commit); production `dpl_5fn2VxqcLUgUkQ7jkvv7DhXfp88B` READY; verified; `dev` fast-forwarded to `bddd833`

Rollback target: `dpl_Gw8vgHxmo3p71vtrpAy14sNtEwoy` — main @ `bf0cb24`, READY, isRollbackCandidate=true
(a docs-only promotion; rolling back would change nothing that serves)

## Shipping (origin/main..origin/dev)
- `20e4890` Promotion record for #93 (#94)
- `d9297d9` Close out session 6: tracker section, handover §6, Doc snapshot link (#95)
- `0801ffe` Rename the #93 promotion record to -15d (#96)
- `1124588` Session 5 close-out: release record, handover, tracker (Stripe promotion codes) (#90)

Five files, all under `docs/handovers/` and `.claude/releases/`. `dev` is not
behind `main`.

## Blast radius
- Migrations: NO · vercel.json: NO · env vars: NO · webhooks: NO · lib/email / crons: NO
- **Runtime code: NONE.** This is a no-op deploy; the `main` build will produce
  a byte-equivalent site. It exists so the handovers and records are on `main`.

## After merge
- Production `dpl_5fn2VxqcLUgUkQ7jkvv7DhXfp88B`, main @ `bddd833`, READY. `main` CI on the merge commit: verify + e2e success.
- www 200 · app 307 → /sign-in · `/api/cron/entitlements` → `{"error":"Unauthorized"}`.
