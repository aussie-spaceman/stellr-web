# Promotion — 17 Sept 2026 (session 7: codebase cleanup)

Status: **Promoted** — #110 merged 2026-09-17 04:32Z as `e8d3904` (merge commit); production `dpl_6rihDvXsoSrPiYhkEbGdLtpQuEZv` READY; verified; `dev` synced to `e8d3904`

Rollback target: `dpl_5fn2VxqcLUgUkQ7jkvv7DhXfp88B` — main @ `bddd833`, READY, isRollbackCandidate=true

## What ships (`origin/main..origin/dev`, 11 commits, 323 files, +876 / −3,943)

| Commit | PR | What |
|---|---|---|
| `be2281d` | #98 | Promotion record for #97 (docs) |
| `cc8b122` | #99 | T1 dead-code deletes + T2 quick fixes (−3,178) |
| `8700422` | #100 | 37 admin-check copies → `lib/admin-auth` |
| `13c6224` | #101 | One Stripe client (`lib/stripe.ts`) |
| `605efe5` | #102 | One HubSpot transport |
| `34811d9` | #103 | `timeAgo`/`slugify`/`sleep` into `lib/utils` |
| `f38258c` | #104 | `isEmailLike` for the five lead gates |
| `5165f86` | #105 | Two swallowed failures now logged |
| `7cd331b` | #106 | Scripts → `supabaseServer()` |
| `7a400cc` | #107 | 14 dead exports gone, 60 un-exported |
| `1ed7a97` | #108 | Session 7 close-out docs |

Full record: `docs/handovers/HANDOVER-cleanup-2026-09-16.md`, TRACKER §7.

## Blast radius

- **Migrations:** none added. `db:status --prod` reports the known TRACKER 4.8 cosmetic pair (`20260910140426/27`, applied 10 Sept under MCP timestamps) — not real pending work.
- **`vercel.json` crons:** untouched.
- **Env vars:** none new. `HUBSPOT_ACCESS_TOKEN` / `STRIPE_SECRET_KEY` reads moved into helpers; `VOLUNTEER_SPACE_SLUG` already on main.
- **Webhook contracts:** Stripe webhook loses its `extra_session` branch. Only the deleted `sessions/purchase` route ever created Checkout Sessions with that metadata, and it has been uncalled since July; Checkout Sessions expire within 24 h, so no in-flight session can arrive. Clerk/DocuSign/Apollo/Motion handlers unchanged.
- **Outbound volume:** unchanged. `lib/email.ts` change is one log line redacting the recipient address.
- **Runtime code, yes:** 41 `/api/admin` routes and the member layout now call `lib/admin-auth` (byte-identical rule, pinned by test); 26 Stripe inits go through `lib/stripe` (same nullable/throwing contracts, pinned by test); three routes and one page now 404 (`sessions/purchase`, `group-join/[token]` GET, `events/[slug]/roster`, `/register-campaign`); `/admin/access` becomes a `next.config.mjs` redirect (query forwarded); 20 orphan public assets + 72 duplicate photo derivatives 404 (in-repo references: none; external HubSpot/Sanity references: unknowable, restore from `68ac4ef^`); `package.json` drops `@sanity/ui`/`@sanity/icons`/`react-is` (still installed transitively via `sanity`) and sets `engines.node >=24` (Vercel runtime is 24.x).
- **Docs/config:** README and CLAUDE.md rewritten; `.nvmrc` 24.

## Verified before promotion

Every PR: CI `verify` (tsc, token lint, migration lint, vitest, full build) + `e2e` confirmed to have actually run (45 passed each time). #99 additionally: local `npm run build` with prebuild gates, tree clean after; Playwright smoke on the worktree incl. a throwaway spec for the redirect, the 404s, kept assets, `coaching/topup`, `/studio`.

## Steps

- [x] 1 — state captured (dev not behind main)
- [x] 2 — blast radius named
- [x] 3 — migrations: none
- [x] 4 — rollback target recorded
- [x] 5 — #110 opened; PR-triggered CI: verify + e2e success (e2e 45 passed, confirmed in the log)
- [x] 6 — user said "yes, merge it"; merged 04:32Z as `e8d3904` (`--merge`)
- [x] 7 — production `dpl_6rihDvXsoSrPiYhkEbGdLtpQuEZv`, main @ `e8d3904`, READY. www 200 / app 307 → /sign-in / cron `{"error":"Unauthorized"}`; `/admin/access?tab=objects` 307 → `/admin/members/access?tab=objects`; `sessions/purchase`, `group-join/x`, `logo-vert.svg`, `academy-1-1200.jpg` 404; `logo-horiz-tight.svg`, `mentors-1-1200.jpg`, `Aileron-Regular.otf`, `/studio`, `/events`, `/membership` 200; `api/admin/access/relations` 403; POST `coaching/topup` 401. Caveat: `www.` did not resolve through this machine's system resolver during the check (`dig` resolved it; both published IPs served 200 with correct SNI) — local DNS, not the site.
- [x] 8 — dev synced, this record marked Promoted
