# Promotion — 18 Sept 2026 (session 8: resumable registration payment)

Status: **In progress** — migration pending on production; promotion PR not yet opened

Rollback target: `dpl_6rihDvXsoSrPiYhkEbGdLtpQuEZv` — main @ `e8d3904`, READY, isRollbackCandidate=true

## What ships (`origin/main..origin/dev`, 3 commits, 31 files, +2,547 / −172)

| Commit | PR | What |
|---|---|---|
| `d4e18a3` | #109 | Promotion record for the 17 Sept cleanup promotion (docs) |
| `3586e09` | — | Merge of `origin/main` into `dev` (post-#110 sync) |
| `2473e5a` | #111 | Resumable registration payment: durable pay link, resume on resubmit, admin send |

Full record: `docs/handovers/HANDOVER-registration-resume-2026-09-17.md`, TRACKER §8.
Prompted by a parent (Colorado SDC, 14 Sept) locked out of an unpaid registration.

## Blast radius

- **Migrations: one.** `20260917160000_registration_pay_token.sql` — adds
  `registrations.pay_token text UNIQUE` and `pay_link_sent_at timestamptz`
  (both nullable, `IF NOT EXISTS`, plus column comments). Additive; old code
  ignores the columns. **Must reach production before the merge** — the new
  register routes insert `pay_token` and would fail against the old schema.
  Applied to dev on 17 Sept (ledger realigned). The two `20260910140426/27`
  rows `db:status --prod` also lists are the known cosmetic pair (TRACKER 4.8).
- **`vercel.json` crons:** untouched.
- **Env vars:** none new.
- **Webhook contracts:** Stripe webhook untouched. Checkout sessions minted by
  the new pay route / resume path carry the same `registrationId` /
  `isGroup` metadata the webhook already matches on.
- **Outbound volume — yes, deliberately:** every paid individual registration
  now sends one "Your registration is saved" email (registrant + emergency
  contact) at submission. A resubmitted form for a pending registration
  re-sends it, rate-limited to one per 10 minutes per registration. Group
  card confirmations carry the pay link (no extra send). Admin can send on
  demand. Nothing cron-driven.
- **Runtime code, yes:** both public register routes (duplicate check
  rewritten; checkout built by `lib/registration-checkout`; cancel URL now
  the pay page), new public page `/register/[slug]/pay/[token]` and route
  `/api/register/pay` (rate-limited 20/10 min per IP), Account → Billing
  "Pay now" (now includes merch add-ons), admin roster button + route, bulk
  reminder email now carries a link. Forms render the new 409 as an info
  panel and follow a `resume` response to Stripe.

## Verified before promotion

#111: CI `verify` (typecheck, token lint, migration lint, vitest, build) every
step success; `e2e` **48 passed** (45 + 3 new, run against CI's seeded DB).
Locally: `npm run build`; browser on the worktree (pay page states, pay route,
409 → email sent + `pay_link_sent_at` stamped, cooldown copy, the real form's
info panel); `e2e/core/registration-resume.spec.ts` 3 passed with the host
pinned. Not exercised: a live Stripe session; the admin button in a browser.

## Steps

- [x] 1 — state captured (dev not behind main)
- [x] 2 — blast radius named
- [ ] 3 — migration to production (SQL shown, approval, apply, `db:status --prod` clean)
- [x] 4 — rollback target recorded
- [ ] 5 — promotion PR opened; CI green
- [ ] 6 — user said yes; merged with `--merge`
- [ ] 7 — production verified (www 200 / app 307 / cron 401; pay page 404 on a bogus token; register form loads)
- [ ] 8 — dev synced; this record marked Promoted; Daniel's pay link sent (TRACKER 8.2)
