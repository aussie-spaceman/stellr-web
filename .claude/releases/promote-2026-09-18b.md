# Promotion — 18 Sept 2026 (b): session 7 follow-ups + session 8 close-out docs

Status: **Merged, deploy pending** — #121 merged 2026-09-18 20:38Z as `2dfdf24` (merge commit) on the user's yes. Vercel created **no** deployment for the push: incident "Elevated Errors Triggering Deployments" opened 20:32Z (Build & Deploy partial outage). Production still serves `481c40c` (`dpl_FkuJSHMkbwCVtBc3ojzEkG1miepK`), verified healthy. Waiting for the incident to clear; if Vercel does not pick the push up on its own, the git-sourced re-trigger is a fresh push to `main` (a new promotion PR carrying this record), never `vercel --prod`.

Rollback target: `dpl_FkuJSHMkbwCVtBc3ojzEkG1miepK` — main @ `481c40c` (#113), READY, isRollbackCandidate=true

## What ships (`origin/main..origin/dev`, 7 commits, 10 files, +230 / −29)

| Commit | PR | What |
|---|---|---|
| `0215351` | #112 | Promotion record for the 18 Sept (a) promotion (docs) |
| `3664edc` | #114 | Session 8 tracker 8.2 + 8.5 (docs) |
| `786249c` | #115 | TRACKER 7.2–7.4: `next-env.d.ts` untracked; campaign doc; **Proposals & email** button on campaign rows |
| `43197cd` | #116 | TRACKER 7.5 verified (docs) |
| `a96cc41` | #117 | Session 8 close-out (docs) |
| `c9d48b9` | #118 | Session 7 close-out (docs) |
| `36ccb75` | #119 | `production-guard.mjs` refuses live Stripe keys (local-dev script + test); TRACKER 7.6/7.8 |

## Blast radius

- **Migrations:** none. `db:status --prod`: only the TRACKER 4.8 cosmetic pair.
- **Crons / env vars / webhooks / outbound mail:** untouched.
- **Runtime code:** one change — `app/(admin)/admin/competitions/[slug]/page.tsx` renders a header button on campaign rows linking to `/admin/campaigns/[slug]`. Admin-only, behind the existing gate.
- **Build:** `next-env.d.ts` is no longer tracked; `next build` regenerates it (CI `verify` built #115 from a fresh clone and passed).
- **Not runtime:** `scripts/production-guard.mjs` runs only under `npm run dev` locally; Vercel never executes it. `.gitignore`, docs, release record.

Production behaviour is otherwise unchanged. This is a docs-and-one-link promotion.

## Verified before promotion

Each PR: CI `verify` + `e2e` (48 passed on #115, #119; 45–48 on the others), e2e step confirmed to have run in the log.

## Steps

- [x] 1 — state captured (dev not behind main)
- [x] 2 — blast radius named
- [x] 3 — migrations: none
- [x] 4 — rollback target recorded
- [x] 5 — #121 opened; CI verify + e2e success (48 passed, confirmed in the log)
- [x] 6 — user said "yes, merge it"; merged 20:38Z as `2dfdf24` (`--merge`)
- [x] 6a — **Vercel incident** ("Elevated Errors Triggering Deployments", 20:32–21:22Z): no deployment was ever created for `2dfdf24`; the account's build queue drained ~22:40Z with every backlog entry CANCELED and still nothing for `main`. The push event was dropped. Production unchanged on `481c40c`.
- [ ] 6b — re-trigger by git: this record lands on `dev` (#120), a second promotion PR pushes `main` again; needs the user's yes like any promotion.
- [ ] 7 — production verified
- [ ] 8 — dev synced, record marked Promoted
