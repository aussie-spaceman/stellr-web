# Promotion — 21 Sept 2026: TRACKER 7.6 close + 18b record (docs only)

Status: **PR opened** — awaiting CI and the user's yes.

Rollback target: `dpl_Dh62QSavcHkJYAaz1fGEaVDJHQp6` — main @ `3ed7248` (#122), READY, isRollbackCandidate=true

## What ships (`origin/main..origin/dev`, 3 commits, 2 files, +7 / −5)

| Commit | PR | What |
|---|---|---|
| `a923d42` | #123 | 18b record marked Promoted + the "merge is not a deployment" note |
| `55e9ddc` | — | dev ← main sync after #122 |
| `96babc8` | #124 | TRACKER 7.6 closed (local Stripe key swapped; guard clean) |

## Blast radius

**None.** Two documentation files. No runtime code, no migrations (`db:status --prod` shows only the TRACKER 4.8 cosmetic pair), no crons, env, webhooks or mail. The deploy is a rebuild of identical application code.

## Steps

- [x] 1 — state captured (dev not behind main)
- [x] 2 — blast radius: none
- [x] 3 — migrations: none
- [x] 4 — rollback target recorded
- [ ] 5 — PR opened, CI green
- [ ] 6 — user's yes → merge (`--merge`)
- [ ] 7 — production deployment exists for the merge SHA + standard checks
- [ ] 8 — dev synced, record marked Promoted
