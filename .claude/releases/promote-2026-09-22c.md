# Promotion — 22 Sept 2026 (c): Checkr adjudication, certification close-out

Status: **In progress** — migration applied to production 22 Sept ~19:0xZ; awaiting the user's yes on #145.

Rollback target: `dpl_GkEowoaY4U5Gj9yypsRcfHcwxe2k` — main @ `b9418ad` (#140), READY, isRollbackCandidate=true

Carried by the promotion PR **#145**, which a parallel session opened for #141–#144. `dev` has since moved to `836c567`, so the same PR now carries #146–#148 as well; its title and body were corrected rather than opening a competing PR.

## What ships (`origin/main..origin/dev`, 7 commits)

| Commit | PR | What |
|---|---|---|
| `8b934bd` | #142 | Promotion record for 22 Sept (a) |
| `0cb563c` | #143 | Plan: `LINKEDIN_ORGANIZATION_ID` set on production (D3 closed) |
| `8ce0598` | #141 | Checkr seed + runbook: real mock PII per candidate; certification results doc |
| `4689ba9` | #144 | Release record for 22 Sept (b) |
| `f28ff3b` | #146 | **Adjudication** — `flagged` state, admin notification, review queue, adjudication record + route + UI. Migration `20260922181924_bc_adjudication`. Also corrects the Checkr dashboard host |
| `db6fbad` | #147 | Remove a `node_modules` symlink committed to `dev`; tighten the ignore rule |
| `836c567` | #148 | Certification close-out: tracker 9.3/9.6/9.10 ticked, 9.11–9.16 added, handover |

## Blast radius

- **Migration:** `20260922181924_bc_adjudication` — six nullable columns, two CHECK constraints, one partial index, two comments on `member_background_checks`. Additive; no data written, nothing dropped; old code ignores the new columns. **Applied to production 22 Sept** after dev, verified (6 columns, 2 constraints, 1 index), ledger realigned to the filename; `db:status --prod` back to only the TRACKER 4.8 cosmetic pair with ledger-only still at 11.
- **Crons / env vars / webhook contracts / outbound mail:** unchanged. No `vercel.json`, `package.json`, `lib/email.ts`, cron, webhook or `proxy.ts` diff.
- **Runtime, admin-only:** the compliance panel gains a `flagged` ("Needs review") state and a Record-decision action; the audit page gains a filter and sorts flagged rows first; the review queue now includes them. Production has **0 rows** in `member_background_checks`, so nothing changes visibly today.
- **New notification:** community admins are notified when a check becomes `referred`. It cannot fire on production — no Checkr credentials are configured there (`checkr: unconfigured`).
- **A `node_modules` symlink is removed from the tree** (#147) and `.gitignore` tightened. Anyone with an existing checkout could not `git pull` until this lands.
- Everything else is documentation.

## Verified before promotion

- Each PR: CI `verify` (typecheck, design-system lint, unit, build) + `e2e`, green by step outcome on the exact head. 81 files / 780 tests after #146.
- #146 verified on the deployed dev app: panel reads "Needs review" with the adjudication action, audit row matches, and the "View report in Checkr" link resolves (`dashboard.checkrhq-staging.net`, HTTP 200 — the previous host served a Cloudflare 1016).
- Certification run evidence: `docs/CHECKR-CERTIFICATION-RESULTS-2026-09-22.md`.

## Steps

- [x] 1 — state captured (dev not behind main)
- [x] 2 — blast radius named
- [x] 3 — migration applied to production and verified; ledger realigned
- [x] 4 — rollback target recorded
- [x] 5 — promotion PR #145 updated to reflect the full set
- [ ] 6 — user's yes; merge with `--merge`; confirm a deployment exists for the merge SHA
- [ ] 7 — production verified (www 200, app 307, cron 401)
- [ ] 8 — `dev` synced; record marked Promoted
