# Promotion — 22 Sept 2026 (d): release-record housekeeping, docs only

Status: **Awaiting merge** — approved for the two docs commits below.

Rollback target: `dpl_AwwfvC2V1qnReCoQcLd1nuDTHfXj` — main @ `16d0eae` (#145),
READY, `isRollbackCandidate=true`, created 22 Sept 20:30Z. Currently serving
production. Re-queried live, not taken from a stored list.

## What ships

| Commit | PR | What |
|---|---|---|
| `e95e5fa` | #150 | Mark the 22 Sept (c) promotion Promoted |
| `36e9e43` | #151 | Mark the 22 Sept (b) record superseded by (c) |
| — | this | This record (added by the promote sequence itself) |

The entire diff is two files, both under `.claude/releases/`:
`promote-2026-09-22b.md` and `promote-2026-09-22c.md`.

## Blast radius

**None.** No runtime code, no migrations, no `vercel.json`, no env vars, no
webhook contracts, no change to outbound mail. Nothing under `lib/`, `app/`,
`components/`, `scripts/`, `supabase/migrations/` or `package.json`. The
deployment this triggers is a rebuild of unchanged source and production
behaviour is identical before and after.

Migration state unchanged: `db:status --prod` still lists the two 10 Sept grade
migrations as `PENDING` by filename. They are applied — verified by content
earlier today (`grade_type` carries `grade_6/7/8` immediately before `grade_9`;
`promote_grades()` handles `grade_6`), ledger rows under the MCP timestamps
`20260910230533`/`20260910230740` (TRACKER 4.8). **Do not re-apply.**

## Why promote docs at all

So `main` and `dev` do not drift, and so the release history on `main` includes
the correction: `promote-2026-09-22b.md` claimed "Awaiting approval" for a
docs-only promotion that never happened in that form, because PR #145 grew to
carry #146–#148 when a parallel session moved `dev`. Until this lands, `main`
carries the misleading version.

## Verified before promotion

- `origin/main..origin/dev` re-read immediately before opening the PR: exactly
  the two commits named above, two files, docs only.
- Production baseline: `www` 200 · `/api/cron/entitlements`
  `{"error":"Unauthorized"}`.
- Rollback candidate re-queried from the Vercel API.

## After the deployment

Site checks only (Step 7). Nothing feature-specific to exercise.

Still outstanding from earlier promotions, unchanged by this one:
`LINKEDIN_ORGANIZATION_ID=66274777` is live but has never been confirmed to be
Stellr's Page — the check is a real add-to-profile click naming **Stellr
Education** as the issuing organisation.
