# Promotion — 22 Sept 2026 (e): Event JSON-LD description fallback, plus close-out docs

Status: **Awaiting merge approval**. Promotion PR not yet opened.

Rollback target: `dpl_9ab2RiJCQ4D15Mqi7WoRvxg51VcT` — main @ `5e2a021` (#154),
READY, `isRollbackCandidate=true`, created 22 Sept 21:38Z. Currently serving
production. Re-queried live from the Vercel API, not taken from a stored list.

## What ships

| Commit | PR | What |
|---|---|---|
| `2f1dfe7` | #155 | Mark the 22 Sept (d) promotion Promoted; record the actual contents |
| `c2a6f9d` | #156 | Capture the Smartsheet form answers field by field (docs) |
| `0713560` | #157 | Close-out: credentials + LinkedIn handover, tracker session 11 (docs) |
| `87e4315` | #159 | Close-out handover: what this session did not finish (docs) |
| `e9d8866` | #158 | Give untagged events a JSON-LD description (Search Console Events warning) |
| — | this PR | This record |

## Blast radius

**Small, public site only.** The only runtime change is #158, in `lib/structured-data.ts` and
`app/(public)/events/[slug]/page.tsx`:
- The Event/Campaign JSON-LD now has a `description` when the Sanity tagline is empty. It falls back to the
  page's meta description. Today only /events/south-dakota-space-design-challenge is affected.
- The fallback meta description reads "an in-person STEM competition" instead of "a in-person stem
  competition". This changes the `<meta name="description">` on untagged event pages.

No migrations, no `vercel.json`, no env vars, no webhook contracts, no outbound mail. Everything else is
under `docs/` and `.claude/releases/`.

Migration state unchanged: `db:status --prod` lists the same two 10 Sept grade migrations as `PENDING` by
filename. They are applied under MCP timestamps `20260910230533`/`20260910230740` (TRACKER 4.8). **Do not
re-apply.**

## Verified before promotion

- #158 CI: `verify` (typecheck, design-system lint, unit tests, build) and `e2e` (56 passed, 1 skipped,
  against CI's own build) — every step `success`.
- Local dev: South Dakota's Event JSON-LD carries the fallback description, and Colorado keeps its tagline.
- Production baseline: every Event/EventSeries node on /events/* and /competitions already has image,
  offers, eventStatus and organizer.url. Only South Dakota lacks `description`.

## After the deployment

- [ ] A deployment exists for the merge SHA and reaches READY
- [ ] `www` 200 · `app` 307 → `/sign-in` · `/api/cron/entitlements` `{"error":"Unauthorized"}`
- [ ] South Dakota's live Event JSON-LD has `description`
- [ ] `dev` synced to `main`
- [ ] Search Console → Enhancements → Events → **Validate fix** (maintainer, in the dashboard)

Content follow-ups (not code): add a South Dakota tagline in Sanity. Check North Carolina's Stripe price ID,
since its Offer has no `price`.
