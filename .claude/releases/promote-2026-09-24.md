# Promotion — 24 Sept 2026: credentials policy + guardian opt-out; DocuSign template fields; admin member invites

Status: **Promoted**. #183 merged 24 Sept 20:16Z as `e2a99e3` (merge commit). Production
`dpl_Decx4PkoAEdACrqPLeCyA7ZhmTBe` is READY for that SHA (www, app, apex aliased).
Approval for migrations + merge was given in-session (24 Sept).

Rollback target: `dpl_5gBb2Ub8PavTa7sQcqL8DJHB9ob4` — main @ `420eb46` (#168), READY,
`isRollbackCandidate=true`, newest production deployment (currently serving). Queried live from the Vercel API.

## What ships (`origin/main..origin/dev`, 13 commits)

| Commit | PR | What |
|---|---|---|
| `d211ea3` | #180 | DocuSign minor template: field-label script + checker; DOB sent as DD-MMM-YYYY |
| `845203b` | #181 | Admin-created members: DOB/gender optional, "activate your account" invite email |
| `306cda6` | #178 | CI: one e2e job at a time repo-wide |
| `749ccf6` | #179 | Email-signature logo at `/email/signature-logo.png` |
| `fe5bd3a` | #177 | Close-out docs (session 13) |
| `ff39554` | #176 | e2e: credentials publish test self-resets |
| `513eb16` | #175 | Docs: Checkr production cut-over |
| `6a02e80` | #174 | Docs: close-out (2) |
| `bce68ad` | #171 | Credentials: Privacy/Terms 23 Sep, guardian opt-out unpublishes, DocuSign read-back + cron |
| `4b5591d` | #173 | Docs: certification run |
| `3d5ba04` | #172 | Docs: Stripe refunds close-out |
| `f2b403b` | #170 | Docs: Stripe refunds handover |
| `b9588f2` | #169 | Docs: 23 Sept promotion marked Promoted |

## Blast radius

- **Migrations (2, both additive / constraint-loosening — old code is safe against them):**
  - `20260923170000_docusign_form_data_read` — `docusign_envelopes.form_data_read_at` + partial index.
  - `20260924120000_member_account_invite` — `members.date_of_birth` / `gender` DROP NOT NULL; `members.account_invite_sent_at`.
  - Both applied on dev. `20260910140426/27` also show "pending" in `db:status --prod` — the known 10 Sept pair recorded under apply-time timestamps; not re-applied.
  - Gap note: between the #181 migration and the merge, a Clerk `user.created` insert with no DOB now succeeds on OLD code (previously failed); such a member is routed to onboarding only once the new code is live. Window is minutes.
- **Cron:** new `/api/cron/docusign-form-data`, daily 09:15 UTC. Reads DocuSign form data for completed minor envelopes (last 7 days) — no mail unless a guardian ticked the opt-out.
- **Env vars:** none new (diff checked against `process.env.*` on main).
- **Webhooks:** DocuSign Connect handler now reads form data on `envelope-completed` (non-fatal on failure).
- **Outbound mail:** new "credential pages made private" email (only on a guardian opt-out); guardian line in the credential-issued email; #181 invite email (admin-triggered, rate-limited).
- **User-facing:** /privacy and /terms (23 Sep banners), credential "who can see this" copy, admin opt-out response field `alreadyPublic` → `unpublished`, admin add-member form.
- **DocuSign templates:** already fixed in production 24 Sept (all three pass `scripts/check-docusign-template.mjs`); not part of the deploy.

## Log

- 24 Sept — record written; migrations verified on dev; rollback target captured.
- 24 Sept — approval given. The MCP production migration was blocked by the auto-mode classifier
  ("Production Deploy"); David ran both migrations + the two ledger rows (file timestamps) in the
  production SQL editor. Verified read-only: `docusign_envelopes.form_data_read_at` and index exist,
  `members.date_of_birth`/`gender` nullable, `members.account_invite_sent_at` exists, ledger rows
  `20260923170000` / `20260924120000` present.
- 20:16Z — #183 merged (`--merge`); head was dev @ `d211ea3`. Deployment `dpl_Decx4PkoAEdACrqPLeCyA7ZhmTBe` READY 20:20Z.
- Verified live: www 200; app 307 → /sign-in; `/api/cron/entitlements` → `{"error":"Unauthorized"}`;
  `/api/cron/docusign-form-data` → 401 (route live, guard on); /privacy and /terms show the
  "Recent update (23 Sep 2026)" banner and Last Updated 23-Sep-2026; no `</strong>` spacing faults on /privacy.

## Still open

- First real minor consent form: confirm `credential_sharing_opt_out` / `form_data_read_at` are set (checkbox value assumed `"X"`).
- First 09:15 UTC run of `docusign-form-data` (25 Sept): check its runtime log for `processed` with no errors.
- Cron registration is not exposed by the Vercel deployment API; the route answering 401 (not 404) shows it deployed — confirm in Vercel → Settings → Cron Jobs if in doubt.
