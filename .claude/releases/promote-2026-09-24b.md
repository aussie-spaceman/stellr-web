# Promotion — 24 Sept 2026 (second): close-out docs for sessions 13–15, Checkr, credentials/DocuSign

Status: **Promoted**. #190 merged 24 Sept 21:30:19Z as `4a60eac` (merge commit), approved in-session.
Production `dpl_3RQ2kFMe2PfzD76n3o1o756gu8SR` is READY for that SHA, `isRollbackCandidate=true`.

Rollback target: `dpl_Decx4PkoAEdACrqPLeCyA7ZhmTBe` — main @ `e2a99e3` (#183), READY,
`isRollbackCandidate=true`, currently serving production. Queried live from the Vercel API (21:09Z).

## What ships (`origin/main..origin/dev`, 6 commits + this record)

| Commit | PR | What |
|---|---|---|
| `7d8786e` | #187 | Close-out: admin-created member invite; TRACKER Session 15 |
| `73c58a3` | #188 | Close-out: Checkr production cut-over; TRACKER 9.8 progress, 9.19, 9.20 |
| `6eeedb7` | #186 | Close-out: email-signature logo; TRACKER Session 14 |
| `258ba36` | #185 | Close-out: credentials policy + DocuSign template fields; tracker 11.5 |
| `c31003c` | #184 | TRACKER Session 13 update (13.1 observed; new 13.5) |
| `9e6ac3a` | #182 | Promotion record for #183 |

## Blast radius

**None at runtime. Production behaviour is unchanged.**
- `git diff --name-only origin/main...origin/dev` touches only `docs/` and `.claude/releases/`.
- **Migrations:** none in the diff. `db:status -- --prod` pending list is only the known 10 Sept pair (`20260910140426/27`, recorded under apply-time timestamps; not re-applied).
- **Crons, env vars, webhooks, outbound mail:** untouched.
- **Deploy:** `scripts/vercel-ignore-build.sh` builds every `main` push on the production project, so the merge still produces a new production deployment. Its runtime code is identical to `e2a99e3`.

## Log

- 21:09Z — record written; rollback target captured; prod ledger read.
- #189 (this record) → `dev` as `9562230`; its CI run 36059728406 passed every step.
- #190 opened with `dev` @ `9562230`. CI run 36060756130 passed Typecheck, Unit tests, Build and Playwright. `dev` was unchanged at merge time.
- 21:30:19Z — #190 merged with `--merge` as `4a60eac`. The Vercel commit status went `pending` at 21:30:26 and `success` at 21:34:36. `list_deployments` by SHA shows `dpl_3RQ2kFMe2PfzD76n3o1o756gu8SR` READY on production.
- Verified live: www 200; app 307 → /sign-in; `/api/cron/entitlements` → `{"error":"Unauthorized"}`.
