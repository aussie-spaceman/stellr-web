# Promotion — 24 Sept 2026 (third): member hard delete removes the Clerk login

Status: **Promoted.** Production is `dpl_4um37DRop7Uupo1kae8GsBX1RFmR`, main @ `bc28af1` (#197). It went READY at
25 Sept 02:49:50Z and includes #192.

This took two pushes to `main`. #195 merged 24 Sept 22:18:13Z as `d0f4130`, but Vercel refused that production build
("Deployment rate limited — retry in 24 hours") and no deployment was created. The limit had cleared by 25 Sept
02:37Z, well under the 24 hours the message said. #197 (content: only this record's update, #196) re-triggered the build
after the maintainer said "try again".

Approved in-session: `/promote` for #195, and "try again" for #197.

Rollback target: `dpl_3RQ2kFMe2PfzD76n3o1o756gu8SR`, main @ `4a60eac` (#190). It is READY and `isRollbackCandidate=true`.

## What ships (`origin/main..origin/dev`, 3 commits + this record)

| Commit | PR | What |
|---|---|---|
| `9741a3d` | #192 | Member hard delete also removes the Clerk login |
| `2901d33` | #193 | Close-out session 16: handover + TRACKER 16.0–16.8 |
| `73c7a8b` | #191 | Promotion record 24 Sept (second): Promoted |

## Blast radius

- **Runtime code: yes, admin-only.** The change is to `lib/deletion/*` and one paragraph in `DeleteEntityButton`. Only a
  **permanent** delete of a **member** now also calls Clerk `users.deleteUser`. It skips staff logins
  (`admin`/`event_manager`) and logins linked to another member row. Soft delete, Deactivate, participants and
  registrations are unchanged.
- **Migrations:** none. `db:status -- --prod` shows only the known 10 Sept pair (`20260910140426/27`, recorded under
  apply-time timestamps).
- **Env vars:** none new. The code uses the existing `CLERK_SECRET_KEY` through `clerkClient()`.
- **Webhooks:** each such delete produces a Clerk `user.deleted` event. `app/api/webhooks/clerk/route.ts` soft-updates rows
  matching `clerk_user_id`. By then the row is gone or about to be, so this is a no-op. See TRACKER 16.7 for the
  archive-snapshot timing.
- **Crons, outbound mail:** untouched.

## Log

- Record written. Rollback target captured; prod ledger read.
- #194 (this record) → `dev` as `b57a489`. CI passed every step.
- #195 opened with `dev` @ `b57a489`. Both CI runs (36065620506, 36065636675) passed every step. `dev` was unchanged at
  merge time.
- 22:18:13Z: #195 merged with `--merge` as `d0f4130`.
- 22:18:17Z: Vercel status `failure`, "Deployment rate limited — retry in 24 hours". No deployment exists for `d0f4130`.
  The dev project's build of `b57a489` completed shortly before. Whether the limit is per project or per account is not
  known.
- Checked live afterwards, all still on the old deployment: www 200; app 307 → /sign-in; `/api/cron/entitlements` →
  `{"error":"Unauthorized"}`.
- The ~22:30Z push of #196's branch was also rate-limited, on **both** projects. So the limit covered the whole account,
  not one project.
- 25 Sept 02:37Z: after the maintainer said "try again", fast-forwarded `dev` to `d0f4130`. `main`'s CI run 36066659622
  on that commit had passed verify and e2e. Then merged #196 as `fa832a2`, and both projects started building it.
- #197 opened `dev` → `main` with `fa832a2`. Both CI runs (36087030712, 36087085836) passed every step.
- 02:47:11Z: #197 merged with `--merge` as `bc28af1`. `dpl_4um37DRop7Uupo1kae8GsBX1RFmR` went BUILDING and then READY.
  The Vercel status was `success` at 02:49:50Z.
- Verified live: www 200; app 307 → /sign-in; `/api/cron/entitlements` → `{"error":"Unauthorized"}`; no runtime errors
  in the 15 minutes after.
- `main` CI run 36087693839 on `bc28af1` passed verify and e2e. `dev` was then fast-forwarded to `bc28af1`.
