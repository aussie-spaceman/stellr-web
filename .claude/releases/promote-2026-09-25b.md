# Promotion — 25 Sept 2026 (second): env-var rebuild + Avery 8395 name badges

Status: **Prepared — migration applied to production; waiting on merge approval.**

Why now: the maintainer set the production env vars `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` / `NEXT_PUBLIC_BING_SITE_VERIFICATION` (whichever apply) and `INDEXNOW_KEY` on `stellr-web`. `NEXT_PUBLIC_*` values and the force-static `/indexnow-key.txt` are fixed at build time, so production needs a rebuild — and a rebuild is a promotion. `dev` had moved on, so this also ships #205 and #206 (another session). Approved in-session: "Promote all, apply SQL".

Rollback target: `dpl_ETJ6S4A7FJnjkHcD8GqAAfqagoCs`, main @ `839b53c` (#203). READY, `isRollbackCandidate=true`.

## What ships (`origin/main..origin/dev`, 3 commits + this record)

| Commit | PR | What |
|---|---|---|
| `003e538` | #204 | Promotion record 25 Sept: Promoted (docs) |
| `d654aa2` | #205 | Close-out session 17: event award certificates handover + tracker (docs) |
| `e0f3a91` | #206 | Name badges: Avery 8395 labels alongside 5392, per-format artwork, name set above the artwork's rule, mentors get badges, event managers see saved backgrounds |

## Blast radius

- **Runtime code: yes, admin-only** (#206) — badge + artwork routes, `EventBadges`, `lib/badge-layout.ts`, `lib/event-pdf.ts`, `lib/uploads.ts`. Public site unchanged in code; it gains the verification meta tags and a live `/indexnow-key.txt` purely from the new env vars.
- **Migrations:** `20260925140000_badge_8395_artwork` — one nullable column `event_settings.badge_8395_artwork_path` + two column comments. Backward-compatible. Applied to production before merge (below).
- **Env vars:** the three above, set by the maintainer on Production only. Not readable by the Vercel connector (403); confirmed after deploy from the live HTML instead.
- **Crons, webhooks, `lib/email.ts`:** untouched.

## Migrations

- Dev: column present (applied outside the ledger — dev `db:status` still lists `20260925140000`).
- Production: applied 25 Sept via Supabase MCP, ledger realigned to `20260925140000`. `db:status -- --prod`: only the historical `20260910140426/27` pair remains.

## Log

- Record written; rollback target captured; migration applied and recorded on production.
