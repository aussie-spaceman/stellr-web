# Promotion — 25 Sept 2026 (third): admin member save fix

Status: **In progress.** Record written; promotion PR not yet opened.

Why now: admins cannot save any member without a grade (every Adult) — "Save failed. Please try again." Production Postgres, 22:11Z: `invalid input value for enum grade_type: ""`. Fixed in #210.

Rollback target: `dpl_Bora4pE4JroBdbz3eLVA3MDTHfTp`, main @ `868a178` (#208). READY, `isRollbackCandidate=true`.

## What ships (`origin/main..origin/dev`, 3 commits + this record)

| Commit | PR | What |
|---|---|---|
| `862b1b4` | #209 | Promotion record 25 Sept (second): Promoted (docs) |
| `22f5686` | #210 | Admin member PATCH stores `''` as NULL — fixes save for members with no grade |
| `e84f96f` | #211 | Close-out session 18: Avery 8395 name badges handover + tracker (docs) |

## Blast radius

- **Runtime code: yes, admin-only** — `app/api/admin/members/[id]/route.ts`, +4/−1. Empty form fields become NULL instead of `''` (grade, tshirt_size, gender, date_of_birth enums/date reject `''`; text fields now store NULL when cleared). Audit diff already treats `''`/NULL as equal.
- **Migrations:** none. `db:status -- --prod`: only the historical `20260910140426/27` pair.
- **Env vars, crons, webhooks, `lib/email.ts`:** untouched.

## Log

- Record written; rollback target captured; no migrations.
