.next/
node_modules/
.env.local
.env*.local
out/
build/
*.tsbuildinfo
.DS_Store
.vercel
storybook-static/

# DocuSign template exports (scripts/docusign-templates.ts) — bulky base64 PDFs,
# regenerate on demand; not a source artefact.
/scripts/docusign-templates/

# Supabase CLI scratch state — rewritten by every `supabase link`, and
# per-machine. Tracking it made every session that linked a project produce a
# spurious diff, and two sessions linking different projects conflict.
supabase/.temp/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/e2e/.auth/

# clerk configuration (can include secrets)
/.clerk/

# Per-promotion progress docs — local state, like .claude/plans/
.claude/releases/
