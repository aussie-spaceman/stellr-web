# Archived — June 2026 member-app redesign handoff

Moved here from the repo root (`design_handoff_app_redesign/`) in the Sept 2026 cleanup.
The package's IA and flows are still a valid reference (see
`docs/DESIGN-SYSTEM-V2-MEMBER-ADMIN-HANDOFF.md`); its tokens, colours and fonts are
superseded by Design System V2 (`design/tokens.json`, root `CLAUDE.md`). The
`assets/`, `screenshots/`, `Stellr Design Review.dc.html` and
`claude-settings.example.json` files (3.1 MB) were dropped; they are in git history
before this move. The `CLAUDE.md` in this folder is the June one and must not be
copied to the root.
